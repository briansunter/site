---
layout: default
meta_title: Recipes
meta_description: My writings about technology, travel, and productivity.
body_class: recipes
title: Recipes
---

<main id="main" class="main flex flex-1 flex-col md:flex-row py-10 px-6 lg:px-0 lg:py-20">

    <section id="container-centre" class="flex-1 container ml-auto mr-auto">

        <div class="posts">
 
            {% for recipeType in collections.recipes.recipeTypes %}
     {% for recipe in collections.recipes.recipeMap[recipeType] %}

                <article class="mb-8 pb-8 border-0 border-gray-200">
                    <h2 class="text-4xl mb-3">{{ recipeType }}</h2>

                    <a class="text-4xl mb-3"><a class="block text-pink-500 hover:text-purple-900" href="{{ recipe.url }}">{{ recipe.data.title }}</a>



                </article>

            {% endfor %}

            {% endfor %}
        </div>

    </section>

</main>
